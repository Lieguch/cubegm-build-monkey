# P5 执行覆盖率基线（tools/qemu_coverage.py --baseline）
#
# 格式：<场景标记>/<label>=<覆盖函数数>        label ∈ factory / rebuild / control
#   ★ 键里**必须带场景标记**（A/B/C）：同一 label 在不同场景下覆盖率天然不同
#     （环境不同 ⇒ 终止点不同），混用一个键会让棘轮在场景之间互相误报。
# 语义：本次覆盖函数数 < 基线 ⇒ qemu_coverage.py 退出码 2（覆盖率回退）并在报告里标 ✗。
#
# ★ 纪律（与 upstream_fingerprint_baseline.txt 同一条）：
#   基线**只能如实记录当前已达成的最低水位**，不得为了过门禁往低调；
#   覆盖率**上升**时把数字改成新的实测值（水位随进度抬升，形成棘轮）。
#
# ---------------------------------------------------------------
# 2026-09-17 首度量（CI `4f1dc161a641` / `a5743b67d3c7`，均为实测值）：
#   场景 A（工厂默认态：driver.so 加载失败 + ui_cn.zip 打不开 ⇒ 停在 mui_setting 崩溃）
#     覆盖专有函数 48 / 223 = 21.5%，字节 30,804 / 122,922 = 25.1%
#   场景 B（注入 key2；实测与 A 逐字相同 ⇒ 注入对行为无影响）
#   场景 C（补 libkms 桩 ⇒ driver.so 加载成功，但 /dev/dri 无法提供 DRM ioctl ⇒ 更早终止）
#     ⇒ 覆盖率反而下降（窗口变浅），这是环境缺口而非实现回归。
# ---------------------------------------------------------------
A/factory=49
A/rebuild=48
B/factory=49
B/rebuild=48
C/factory=7
C/rebuild=6
