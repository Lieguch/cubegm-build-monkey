# P5 执行覆盖率基线（tools/qemu_coverage.py --baseline）
#
# 格式：<label>=<覆盖函数数>      # label ∈ factory / rebuild / control
# 语义：仅当 --baseline 传入且该 label 有值时生效；
#       本次覆盖函数数 **< 基线** ⇒ 退出码 2（覆盖率回退），并在报告里标 ✗。
#
# ★ 纪律（与 tools/upstream_fingerprint_baseline.txt 同一条）：
#   基线只能**如实**记录"当前已达成的最低水位"，不得为了过门禁而把数字往低调。
#   覆盖率**上升**时：把数字改成新的实测值（水位随进度抬升，形成棘轮）。
#
# ⏳ 2026-09-17：覆盖率度量刚接入（本轮首跑），实测量级待 CI 回来后回填。
#    在回填之前本文件只有注释 ⇒ 不施加任何门槛（不影响现有门禁）。
