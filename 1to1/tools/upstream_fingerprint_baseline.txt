# 上游符号指纹 **基线豁免**（tools/scan_upstream_fingerprint.py --baseline）
#
# 语义：本表列出的符号，即使工厂 vs 重建的 size 比值 ≥ FAIL 阈值，也只判 KNOWN（不拖红 CI）。
#
# ★★ 纪律（违反即门禁失效）：
#   1. 每修好一项，**必须立即删除**对应基线行；
#   2. **不得**把新发现的问题塞进基线（基线只登记"已定位、有明确修复计划、暂未动手"的项）；
#   3. 每行必须带原因与登记日期，便于审计。
#
# 格式：<mangled 或朴素符号名>   # <原因 / 登记日期 / 状态>
#
# ---------------------------------------------------------------
# 2026-09-16 登记（第七个真实分歧：上游 XUnzip 版本不符）
#   以下三项均为「工厂=薄封装，重建=完整实现」模式，已定位为
#   "工厂 = Hans Dietrich「XUnzip.cpp Version 1.3」系 + 厂商定制"，修复方案见
#   STATUS.md 第二十九轮「续接清单」第 2 条（以 calibre 版为基逐函数对齐）。
# ---------------------------------------------------------------
_ZN6TUnzip5UnzipEiPvjj               # 工厂 28 B(+.part.7 488 B) vs 重建 1608 B；待对齐 TUnzip::Unzip
_ZN6TUnzip3GetEiP8ZIPENTRY           # 工厂 152 B(+.part.5 888 B) vs 重建 1208 B；待对齐 TUnzip::Get
_ZN6TUnzip4OpenEPvjj                 # 工厂 84 B(无 GetCurrentDirectory) vs 重建 204 B；待对齐 TUnzip::Open
_ZN6TUnzip5CloseEv                   # 工厂 68 B vs 重建 168 B；待对齐 TUnzip::Close
mxmlSaveFile                         # 工厂 108 B vs 重建 3704 B(34.3×)；疑 minixml 版本不符，下一轮查
mxmlSaveString                       # 工厂 164 B vs 重建 3624 B(22.1×)；同上
