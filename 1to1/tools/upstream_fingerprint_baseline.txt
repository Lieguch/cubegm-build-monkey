# 上游符号指纹 **基线豁免**（tools/scan_upstream_fingerprint.py --baseline）
#
# 语义：本表列出的符号，即使工厂 vs 重建的 size 比值 ≥ FAIL 阈值，也只判 KNOWN（不拖红 CI）。
#
# ★★ 纪律（违反即门禁失效）：
#   1. 每修好一项，**必须立即删除**对应基线行；
#   2. **不得**把新发现的问题塞进基线（基线只登记"已定位、有明确修复计划、暂未动手"的项）；
#   3. 每行必须带原因与登记日期，便于审计。
#
# 格式：<mangled 或朴素符号名>   # <原因 / 登记日期 / 状态>
#
# ---------------------------------------------------------------
# 2026-09-16 登记（第七个真实分歧：上游 XUnzip 版本不符）
#   ★ 已修复 1 项：`_ZN6TUnzip4OpenEPvjj` —— 204 B → **80 B**（工厂 84 B，比值 1.05 ⇒ 自动移出列表）。
#     修法：`unzip.cpp` 新增 `extern ZRESULT zopenerror asm("_ZL10zopenerror")`（地址由
#     `factory_image.S:1904` 的别名 `__f_bss_base+0xa0` = 0x3b2218 提供）；
#     `unzOpenInternal` 按工厂置 4 处 `zopenerror`；`TUnzip::Open` 还原为工厂形式（结尾 `return zopenerror;`）。
#     基线行已按纪律删除。
#   剩余待对齐：以下 TUnzip 三方法（工厂为「薄封装 + .part.N 代码分割」，需逐函数对齐）。
# ---------------------------------------------------------------
#   注：工厂 `Get` = 152 B **+** `.part.5` 888 B = 1040 B，重建 1208 B（1.16×）⇒ 差异不大，
#       主要是 GCC 的冷热代码分割；`Unzip` = 28 B + `.part.7` 488 B = 516 B vs 重建 1608 B（3.1×）
#       才是真正需要对齐的。基线按「符号名」匹配，故两者都列出。
# ---------------------------------------------------------------
_ZN6TUnzip5UnzipEiPvjj               # 工厂 28 B(+.part.7 488 B) vs 重建 1608 B；待对齐 TUnzip::Unzip
_ZN6TUnzip3GetEiP8ZIPENTRY           # 工厂 152 B(+.part.5 888 B) vs 重建 1208 B；待对齐 TUnzip::Get
_ZN6TUnzip5CloseEv                   # 工厂 68 B vs 重建 168 B；待对齐 TUnzip::Close
mxmlSaveFile                         # 工厂 108 B vs 重建 3704 B(34.3×)；★ 已核实**非版本问题**：mini-XML 大函数
mxmlSaveString                       #   mxml_load_data 5452 vs 4812(1.13×) 高度吻合；差异源于工厂走共用的
                                     #   `mxml_write_string`(172 B) 而重建把逻辑内联 ⇒ 保持基线，勿据此改上游。

