# 专有函数等价性基线（tools/prop_equiv.py --baseline）
#
# 语义：本表列出的函数，即使被判 FAIL，也只降级为 **KNOWN**（不拖红 CI）。
#
# ★★ 纪律（违反即门禁失效）：
#   1. 每修好一项，**必须立即删除**对应基线行（`prop_equiv.py` 会自动列出"已不再 FAIL"的陈旧项）；
#   2. **不得**把新发现的问题塞进基线（基线只登记"已定位、有明确修复计划、暂未动手"的项）；
#   3. 每行必须带原因与登记日期，便于审计。
#
# 格式：<函数名>   # <原因 / 登记日期 / 状态>
#
# 判据说明：size_ratio = 重建字节数 / 工厂(指令数×4)；WARN>=1.6，FAIL>=2.5。
#   薄函数（工厂 <=16B，空桩）与校准锚点（__libc_csu_*）**不在此表**，它们由工具自动隔离。
#
# ---------------------------------------------------------------
# 2026-09-20 首次建立（工具首跑：213 个专有函数 → FAIL 7 / WARN 4 / OK 184 / THIN 14 / CALIB 2）
#   实测分布（>=64B，n=178）：p50=1.00 p75=1.09 p90=1.29 p95=1.64 max=4.17
#   ⇒ 基线只登记 7 个真 FAIL（远超 p95 的项）。
#
# 2026-09-20 登记（7 项真 FAIL，均「已定位、待逐个人工核对反汇编」）
Convert_Stereo      # 56B → 412B (7.36x)  ★最大偏差；疑"工厂为特化小函数 / 我们写成通用循环" / 2026-09-20 待查
gameType            # 60B → 276B (4.60x)  疑实现路径不同 / 2026-09-20 待查
SPI_RR              # 92B → 384B (4.17x)  SPI flash 读；疑工厂用固定位操作、我们用循环 / 2026-09-20 待查
outputblankxy       # 220B → 644B (2.93x) / 2026-09-20 待查
DrawSelectBar       # 168B → 488B (2.91x) / 2026-09-20 待查
SPI_WW              # 104B → 296B (2.85x) SPI flash 写；与 SPI_RR 同族 / 2026-09-20 待查
mui_LoadUIResource  # 324B → 876B (2.70x)  UI 资源包加载；疑未按工厂的"条目缺失"路径裁剪 / 2026-09-20 待查
#
# 另记（非 FAIL，不计入基线；单独跟进）：
#   MISSING: code_convert.constprop.22 / mui_outputxy_length.isra.19（归一化后重建侧仍无同名 FUNC 符号
#            ⇒ 疑被内联或命名不同，需查 src/proprietary 是否真的有这两个实现）
