

### 第 48 轮：打开输入子系统（场景 F）—— `mui` 57% 重构量的唯一入口

**问题**：第 47 轮拿到 M7（菜单存活）后 stdout 停在 `js0 Opened!`。查到最底：
shim 把 `/dev/input/*` 重定向到 `/dev/zero` ⇒ `ReadUSBJoy` 的 `read(fd,buf,8)` 读回 8 个零字节
⇒ `type=0` ⇒ 直接 return 旧值 ⇒ **菜单永远收不到输入** ⇒ `mui`（42 函数 / 70396 B = 重构量 57%）零覆盖。

**实现**（最小侵入）：命中 `/dev/input/js<N>` 且 `CGM_INPUT_HEX` 非空时，
`memfd_create` 造内存 fd + 预置 `js_event` 字节流 + `lseek` 回 0 再返回。
★ **刻意不拦 `read`/`write`** ⇒ guest 读语义 100% 原生，也不牵扯 stdio 内部（已踩过 stdio 再入）。
顺带接管 `access()`（guest 确实导入），消除"设备节点是否存在"这个宿主环境变量。

**事件格式**：`struct js_event` 8 字节小端 `[time u32][value i16][type u8][number u8]`；
`0000000001000100` = 按下 button 0。×4 后 EOF ⇒ 返回旧值 ⇒ 等价"持续按住"。

**场景 F**：`CGM_TIMEOUT=30 CGM_KEY2_SEED=1 CGM_KEY2_HOOK=1 CGM_IO_TRACE=1 CGM_COV_TAG=F
CGM_INPUT_HEX=<64 字符>`；观测项（`|| true`）。`1to1-qemu-behav` 现 5 场景。

**附带修两处**：
① `behav_diff.py` 加**方向标注**（文档承诺过、代码里没有 ⇒ 让文档成真），
   已用真实制品验证（E 出标注 / A 不出）；
② shim 的 key2 探针文案写着**第 42 轮修正前**的旧注入值 `PK\x06\x06` ⇒
   **误导性证据**（会让"现值=注入值"被误读成"未生效"），已改动态口径。

**导出符号核对**：`access`/`open`/`open64`/`openat`/`fopen`/`fopen64`/`mmap`/`munmap` 均导出；
`read` **不在表内**（正是本方案的安全点）。

**读 `.dynsym` 的坑**：符号名必须用 `.dynsym` 的 **`sh_link`** 指向的 `.dynstr`，
**不是** `e_shstrndx`（后者是 section 名字符串表）。用错会得到 `.dynsym`/`.hash` 这类垃圾名，
据此误判"符号没导出"。

### 第 48 轮·补：场景 F 首跑 + ★ 一次自我引入的事故（三场景静默失效）

**场景 F 首跑**：注入生效（`open(js-inject)` 覆盖 js0..js3，stdout 23→**25** 行），
但**覆盖率与里程碑未变**（68/223、M7）⇒ **输入尚未驱动菜单逻辑**，下一轮继续。
副作用：`access` 接管过宽（4 个 js 设备全"在线"）⇒ 应限定编号。

**★ 事故**：CI 三 workflow 全 success，但 B/E/F 的 `behav_diff.txt` 末行是
`json.decoder.JSONDecodeError: Invalid control character` ⇒ **三场景判定不存在**。
根因 = 我写的 key2 探针文案含 `PK\x05\x06`（真转义 ⇒ ENQ/ACK 控制字节）
⇒ `behav_capture.sh` 收进 JSON ⇒ 非法。而脚本只 echo 退出码、B/E 又是 `|| true` ⇒ 静默。

**修复**：① 文案改十六进制文本；② 新增第 15 道 ★ 门禁 `tools/check_shim_charset.py`
（判据区分反斜杠奇偶、构造性自证 + 反向验证）；③ `behav_diff` 退出码非 0/2/3 ⇒ `::error::`+`exit 1`。

**铁律 129**：探针输出必须可安全序列化；仪器崩溃必须与"被测对象失败"分开显式化 ——
否则出现最危险的假绿（报 success 而什么都没判）。

### 第 48 轮·再补：注入设施两次改进

- **`CGM_INPUT_JS`（默认 "0"）**：`access` 只对**在线的** js 编号返回可读 ⇒ 消除"4 个手柄全在线"
  这个不符真机的假象（真机只插一个）。`open` 特判同步按编号，避免两路口径不一致。
- **`CGM_INPUT_FILL=<字节>`**：把事件流**循环填充**到指定长度 ⇒ 按键覆盖整个运行窗口，
  而不是"启动瞬间闪一下就 EOF"。场景 F 用 4096 B ⇒ 256 个"按下+松开"周期 ⇒ ~7.7 秒。
- 场景 F 现参数：`CGM_INPUT_JS=0` / `CGM_INPUT_HEX=0000000001000100 0000000000000100` / `CGM_INPUT_FILL=4096`。
- 下一轮看：菜单是否出现动作、覆盖率是否从 68/223 起涨、里程碑是否有新阶段。

## 第 49 轮（09-19）：起始屏幕根因定案 + 场景 G/H

- ★★★ **根因定案**：`main_Menu` = `switch(menulog[0])`；`golden/menu.log` 头 u32 = `5`
  ⇒ 开机即停在 `mui_setting()` 轮询 ⇒ E/F「活着(exit=124) 但覆盖率恒定 68/223」。
  吻合证据：`mui_setting` 已覆盖 ✓，5 个屏幕函数（26,216 B）全未执行。
- 输入注入**端到端已验证**：strace `read(js0,8)=8`、`access(js1..3)=-1 ENOENT`、
  shim `16 B -> 4096 B（256 周期）`、`掩码 = 0x1`。但注入 `number=0` = **SELECT**（非导航键）
  ⇒ 语义无效（假阴性）。键位真值源 = `joystick.zip/ui.cfg`：
  0=SELECT 1=DOWN 2=UP 4=LEFT 8=START 16=RIGHT 17=A 18=B。
- 资源查证：`ui_cn.zip` **无 `font.ttf`**（顶层文件 1,840,376 B）⇒ `find font.ttf ... fail` 是正常回退；
  `filelist.xml`(8,654 B) **非空**（真实游戏列表）；`cores/` 仍无 `.so` ⇒ stub core 仍是下一入口。
- 我们侧优于工厂：工厂报 `find ui.cfg` / `find setting.raw` **fail**，
  我们 `DBGUNZ size=2857048` 与 zip 元数据**逐字节一致**。
- 新设施 `CGM_MENULOG_SCREEN=<0..5>`（只改头 4 字节）；新场景 **G**（screen 0，vs E）/
  **H**（+DOWN/A 注入，vs G）。本地干跑三情形（未设 / 设 0 / 设 9）全对。
- 修缺陷：E 有 `CGM_DBGUNZ`/F 没有 ⇒ 跨场景差异不可归因。已给 F 补上。
- `MEMORY.md` 精简（约 9,600 → 6,151 字符）：删掉与技能铁律重复的 4.4 章，更新全部硬数字。

### 第 49 轮·续（09-19 上午）

- ★★★ 场景 G **成功打开 `mui_menu` + `mui_do_file_list`**（从未执行过），`menu.log` 注入证据 6 条。
  但覆盖率 68→59 且我们侧 exit 124→139：**是换到了一条更早崩的路径**。E→G 精确集合变化：
  新增 2（`mui_menu`、`mui_do_file_list`）／消失 11（`mui_setting` 链含整个摇杆轮询 +
  `mui_outputxy_t`）。
- ★ 副产品：**摇杆轮询循环在 `mui_setting` 里，不在 `main_Menu` 顶层** ⇒ 解释了 H 新增 0 个。
- ★★★ 新崩溃根因：`/sdcard/root.dat` 缺失（它是 **ZIP**，内含 `fileinfo.txt`）。工厂
  `00_rkgame_ALL.c:19268-19285` 的失败分支让 `DAT_003af2ac` 保持 NULL，随后
  `mui_do_file_list(iVar11, DAT_003af2ac)` 解引用它 ⇒ 我们崩在 `mui_do_file_list+0xf0` 的
  `ldrb r0,[r0]`（r0=0），工厂同形态。**环境缺口，非缺陷。**
- ★ 新设施 `CGM_ROOTDAT=1`（合成 `/sdcard/root.dat`＝ZIP{fileinfo.txt ← 原厂 49 B fileinfo}）；
  新场景 **I**（= G + CGM_ROOTDAT，严格单变量）。本地三情形干跑全对。
- ★ 三处仪器/流程缺陷修复：① 短 sha 传给 `head_sha` **静默返回空集**（11 次「（无）」不报错）；
  ② E 有 `CGM_DBGUNZ`、F 没有 ⇒ E/F 差异不可归因；③ **提权重试会让整条命令从头再执行一次**
  ⇒ 副作用执行两次（cp 备份被覆盖、幂等断言误报、推送第二次早退）。

### 第 49 轮·三（09-19 中午）

- ★★★ 场景 I（补 `/sdcard/root.dat`）**大成功**：我们侧专有函数覆盖 **59 → 76/223 = 34.08%**（历史最高，
  此前最好 68）；覆盖函数 189 → **225/1661**；M7 复现；exit 回到 124（活着）；
  **I 相对 G 新增 17 / 消失 0**，且 `exec_set_diff` 报"**仅工厂执行 = 0 个**"（我们是工厂的超集）。
- ★ 新缺口定位到行：`open /sdcard//.dat fail` ← `mui_DisplayThumbnail.c:40-42` 的
  `mui_extract_basepath(...)` + `sprintf("%s/%s/%s.dat", root_path, basepath, basepath)`；
  basepath 取 `strrchr('/')` 之前 ⇒ 无 `/` 即空 ⇒ `//.dat`。
- ★★ `fileinfo.txt` 真值源读出：分隔符 `,`/`;`/`\n`/`\r`；每项经 `mui_extract_basepath` 后
  **被 `strtol(...,10)` 当十进制数** ⇒ 必须形如 `NNN/<文件名>`。`cores/filelist.xml` 的
  `name="002/xxx.zip"` 正是该形态 ⇒ 新工具 `tools/make_rootdat.py --mode filelist` 提取 **135 项**。
  ⇒ 场景 I 用的 `golden/fileinfo`（25 个 `0`）已判定**不成立**（保留作对照）。
- ★ 新增场景 **J**（= I 的 `CGM_ROOTDAT` 1→2，严格单变量）；本地端到端干跑全绿。
- ★ 又修一处可移植性缺陷：`pwd` 给的 `/d/...` 被 Windows python 当 `D:\d\...`；改相对路径 +
  `python3→python` 解释器回退。

### 第 49 轮·四（09-19 下午）

- ★ 场景 J **否证**：把 `fileinfo.txt` 换成真值源（`filelist.xml` 的 135 项）后，
  `/.dat fail` 仍 29 行、专有函数覆盖仍 76/223 ⇒ **`root.dat` 的内容对当前路径零影响**
  （作用仅是"让 `DAT_003af2ac` 非 NULL 从而不崩"）。I/J 覆盖集合**逐项完全相同**。
- ★ 真因定位：`file_info_list` 还有一条 readdir 填充路径（`dir_serial_list`，拷贝 `d_name` 并记 `d_type`），
  而沙箱没有 `000/002/004` 游戏目录 ⇒ 列表为空 ⇒ basepath 空 ⇒ `/sdcard//.dat`。
- ★ 下一轮目标（已量化）：`CGM_GAMEDIRS=1` 合成 3 个目录（`000` 105 项 / `002` 21 项 / `004` 9 项）
  + 135 个占位游戏文件。
- ★ 附带发现：`libemu_fbafast.so` 被 filelist 引用但 `config.xml` 未注册。

### 第 49 轮·K（09-19 下午）

- ★ 实现 `CGM_GAMEDIRS=1` + 新工具 `tools/make_gamedirs.py`：按 `cores/filelist.xml` 的 135 个 `name=`
  合成 `/sdcard/cubegm/000|002|004/`（105/21/9 项）+ 占位游戏文件（22 B 合法空 ZIP），并自证 readdir 可见。
- ★ 缺省时**显式清理**残留（开关缺省 ⇒ 无 `NNN/` 目录 = 场景 J 的预期状态）。
- ★ 判据改为**看日志路径内容**（`/sdcard//.dat` → `/sdcard/NNN/NNN.dat`），因为覆盖率会被别的因素抵消。
- 新场景 **K**（= J + `CGM_GAMEDIRS=1`，严格单变量）；本地干跑全对。
