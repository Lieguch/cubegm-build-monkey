
### 第四十四轮：setting.raw 判决 + 关键地址命中普查 + 三处仪器静默失效

- **判决**：`ui_cn.zip` 里 **确实有 `setting.raw`（2,857,048 B）**，我们侧探针显示
  `zr=0 HIT size=2857048` ⇒ **我们找到且解压正确**；工厂对同一 zip 里存在的条目全报 `find … fail`。
- ★ **新设施 `CGM_TRACE_ADDRS`**（在 `-d exec` 原始日志被删前统计指定地址命中；制品只留尾部 800 行）：
  工厂侧 `000119f4`（`unzLocateFile` 的 -99 早退，`[24]==0`）= **1**、`0001162c`（`unzGoToFirstFile`）= **1**、
  而 `00010ea0`（`unzStringFileNameCompare`）= **0**、`000115ec`（`unzGetCurrentFileInfo`）= **0**
  ⇒ **通过了 [24] 检查但 `unzGoToFirstFile` 返回非 0 ⇒ 遍历体零执行**，
  另一次在 `[24]==0` 处早退。⇒ 工厂失败钉在"中央目录遍历"层，**从未比过名字**。
  ⇒ 归因沙箱环境缺一环；真机 `menu.log` 证明能打开资源包 ⇒ **不影响替代，留待真机对照**。
- **三处静默失效（都已修）**：
  ① `set -e` + `_n=$(grep -c …)` 无匹配返回 1 ⇒ 脚本整体退出（`trace_hits` 空、rebuild 侧没跑、log 没删）。
  ② YAML `run:` 把**注释插进 `\` 续行链** ⇒ 链断 ⇒ `CGM_KEY2_SEED`/`KEY2_HOOK`/`IO_TRACE`/`DBGUI2`
     静默丢失 ⇒ 重建侧退回 `open … fail`，而**行为门禁假绿 PASS**。新增门禁
     `tools/lint_workflow_continuation.py`。
  ③ Python heredoc 里 `\\n` 被 Bash 工具折叠成真换行 ⇒ 含反斜杠的锚点永不匹配 ⇒ 改用 `chr(92)`；
     连带发现仓库有 **181 个 CRLF 文件**，多行替换前必须换行归一。
- 探针按纪律已撤（`DBGUI2` 残留 0）；`1to1-verify` 现 **11 道硬门禁**。

### 第 45 轮：C++ 包装层对齐（XUnzip/TUnzip）+ 一处自我更正 + 第 12 道硬门禁

- **更正**：第 44 轮报"`TUnzip::Find` 缺 `unzCloseCurrentFile`"是**错的** —— 全 ELF 0 个 `bl`，
  但尾部机器码与独立函数体逐指令同构 ⇒ 同 TU 内联。判据必须回到机器码语义（铁律 112 二次应用）。
- **真差异 1（已修）**：我们 `TUnzip::Find` 多一个 `char name[MAX_PATH]` + `strcpy`
  （工厂 `bl unzLocateFile` 前 r1 从未被写 = 直接透传）⇒ 语义多余 + **潜在栈溢出**
  （>263 名字在 `UNZ_MAXFILENAMEINZIP` 检查前冲帧）。删除后 `0x120→0x100`，`strcpy`/`#264` 消失。
- **真差异 2（已修）**：`unzOpenCurrentFile` 工厂**单参** `_Z18unzOpenCurrentFileP5unz_s`，
  我们双参 `..._sPKc` ⇒ 收成单参（定义/声明/2 调用点），mangled 名**完全一致**。
  附带收益：C++ 强类型在**编译期**拦住"调用点少传参"（反向验证实测触发）。
- **新门禁** `tools/scan_cxx_abi.py`：Itanium mangled 名对拍（参数个数+类型全在名字里，
  与编译器无关）。归一化 `.isra/.part/...`；排除 `_ZL/_ZZ`；判据收窄 + 棘轮；
  自证 3+1；反向验证精确报出唯一一条。首跑：共有 62 名，**签名不一致 0**。
- **新发现**：我们独有 5 个 C++ 函数（4 个加密残留 + `EnsureDirectory`）⇒ 工厂疑似删掉
  `TUnzip::Unzip` 的 ZIP_FILENAME 分支（与尺寸 516 B vs 1600 B 互证）。入台账，下一轮处理。
- 本地 8 道门禁全绿；`1to1-verify` 现 **12 道 ★**；提交 `eefe0a68`。
- 踩坑：Bash 工具折叠 `\\` 导致 heredoc 里含反斜杠的锚点失配（铁律 117 自己又踩一次）；
  临时 `.cpp` 编译需显式 `-Isrc/upstream/xunzip`（否则 `#include "unzip.h"` 找不到）。
