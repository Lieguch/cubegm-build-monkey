# 2026-09-23 工作日志

## AC Git 迁移 + 两条 CI 红的根因修复 + 工具链判决实验

**用户新任务**：学习 https://git.acwing.com/help 的 CI、迁移 GitHub 项目到 `lieguch/cubegm-rkgame`、仓库管理、继续提交构建。

- **平台探明**：AC Git = **GitLab 14.2.3-ee**。密钥有效（项目机器人 `project_45404_bot`，角色 Maintainer）；
  `/help` 对 API token **401**（只认浏览器会话）⇒ 无法程序化学习帮助页。
- **CI 判决**：**实例无任何 Runner**（`/runners`=[]；untagged + 10 种 tag × 25 分钟无人领走）⇒
  `.gitlab-ci.yml` 就位但不会执行；**继续构建仍需 GitHub Actions**，或先注册 Runner。
- **迁移完成**：1282 文件 / 42 目录；`factory.rkgame.bin` 设 100755；`.pat`/`push_1to1.py` 不在库（安全核对通过）。
  新增 `tools/sync_acgit.py`（清单复用 `push_1to1.py --list-only`，dry-run 实测精准）。
- **CI 红复盘（我引入的）**：
  ① `1to1-verify`：`volatile` 加在全局 ⇒ 严格口径 2 处类型错（`sfc_request:155` / `sfc_uninit:14`）。
     修法：设备块指针 volatile、RAM 游标不加、非访问点显式转型、**拆分被 Ghidra 复用的 `puVar5`**（新增 `puDev`）。
     本地复跑：宽松 213/213、严格 213/213、**假绿 0**。
  ② `1to1-qemu-behav`：`mmio_width_audit` 只找 `.json` 而仓库只有 `.gz` ⇒ 建 `tools/factory_data.py`
     共享加载器；并做 **CI 近似态自证**（藏掉 `.json` 后全部门禁仍 exit 0）。
- **判决实验（2 分钟消掉一个方向）**：GCC 13.3 的 ±15% 命中 **1.9%**、中位比 0.553、直方图 L1 2.000；
  clang 21 为 **77.6% / 0.965 / 0.443** ⇒ **换 GCC 不是提升保真度的方向**。
- 又踩两条 Windows 特有 git 坑：`fetch` 未带凭据导致 `origin/main` 陈旧（push 被拒）；
  Windows 无 exec 位 ⇒ 必须 `git update-index --chmod=+x`。
